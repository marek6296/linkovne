# Supabase — project "ai news" (shared auth with the other sites)
NEXT_PUBLIC_SUPABASE_URL=https://dmxosdgvmzvkeivknczv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...

# Public URL of the site — used for OAuth redirects and OG images.
NEXT_PUBLIC_SITE_URL=https://linkovne.com

# Domain shown in the UI (domain/username). Defaults to linkovne.com in code.
# NEXT_PUBLIC_SITE_DOMAIN=linkovne.com

# --- server only (never NEXT_PUBLIC_) ---
# ANTHROPIC_API_KEY=sk-ant-...
# SUPABASE_SERVICE_ROLE_KEY=...        # link/domain health crons + lead emails
# RESEND_API_KEY=re_...                # lead + domain-down emails
# LEAD_FROM_EMAIL=Linkovne <hello@linkovne.com>
# VERCEL_TOKEN=...                     # custom domains
# VERCEL_PROJECT_ID=prj_...
# VERCEL_TEAM_ID=team_...
# CRON_SECRET=...                      # protects /api/cron/*

# --- Stripe (subscriptions) — fill in once the Stripe account exists ---
# STRIPE_SECRET_KEY=sk_live_...        # or sk_test_ while testing
# STRIPE_WEBHOOK_SECRET=whsec_...      # from the webhook endpoint /api/stripe/webhook
# STRIPE_PRICE_PRO=price_...           # monthly Pro price id
# STRIPE_PRICE_BUSINESS=price_...      # monthly Business price id
